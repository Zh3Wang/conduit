# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/apps/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Article API
    description: The greeting service definition.
    version: 0.0.1
paths:
    /article:
        post:
            tags:
                - Article
            description: Sends a greeting
            operationId: Article_GetArticle
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetArticleRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetArticleReply'
components:
    schemas:
        ArticleData:
            type: object
            properties:
                slug:
                    type: string
                title:
                    type: string
                description:
                    type: string
                body:
                    type: string
                tagList:
                    type: array
                    items:
                        type: string
                createdAt:
                    type: string
                updatedAt:
                    type: string
                favorited:
                    type: boolean
                favoritesCount:
                    type: integer
                    format: int32
                author:
                    $ref: '#/components/schemas/ArticleData_authorInfo'
        ArticleData_authorInfo:
            type: object
            properties:
                username:
                    type: string
                bio:
                    type: string
                images:
                    type: string
                following:
                    type: boolean
        GetArticleReply:
            type: object
            properties:
                article:
                    $ref: '#/components/schemas/ArticleData'
            description: The response message containing the greetings
        GetArticleRequest:
            type: object
            properties:
                slug:
                    type: string
            description: The request message containing the user's name.
tags:
    - name: Article
